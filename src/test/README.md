Tests intentionally removed.

The project previously contained automated integration/unit tests, but they were removed per project decision. If you want to re-enable tests later, add test classes under `src/test/java` and update `pom.xml` dependencies accordingly.
